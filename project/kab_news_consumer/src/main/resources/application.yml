server:
  port: 9093
spring:
  application:
    name: kab-news-consumer
  mvc:
    view:
      prefix: /templates/
      suffix: .html
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/
feign:
  hystrix:
    enabled: true